import { BookError, BookingModel, BookModel, BookModelResponse,DeliveryModel, HistoryModel } from './book.model';
import { OneBookModel,OneBookResponse } from './one-book.model';

export type {
    BookingModel, DeliveryModel, HistoryModel, BookModel, BookError, BookModelResponse, OneBookModel, OneBookResponse
}